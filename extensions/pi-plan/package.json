{
  "name": "@duskpi/pi-plan",
  "version": "0.1.0",
  "private": true,
  "description": "Vendored pi-plan extension for duskpi: read-only planning mode with approval-based execution",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check": "bun run typecheck && bun run test"
  },
  "devDependencies": {
    "@mariozechner/pi-coding-agent": "^0.57.1",
    "bun-types": "^1.3.10",
    "typescript": "^5.9.3"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  }
}
